// Configuration for SASS preprocessor
// VS code will complain if this file isn't present... also it won't work
const sveltePreprocess = require("svelte-preprocess");

module.exports = {
    preprocess: sveltePreprocess(),
};
